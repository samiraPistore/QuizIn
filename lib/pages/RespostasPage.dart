import 'package:flutter/material.dart';

class RespostasPage extends StatelessWidget {
  const RespostasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}